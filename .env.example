# コピーして .env を作り、実値を入れる（.env は gitignore 済み）。
# 実値は CLAUDE.local.md と一致させること。

# the Spark の WSS エンドポイント（tailscale serve で終端した正規 HTTPS/WSS）
SPARK_WSS_URL=wss://spark-host.<tailnet>.ts.net:8000/translate
